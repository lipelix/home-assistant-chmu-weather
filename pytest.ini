[pytest]
# The Home Assistant test fixtures are async. Only tests/test_weather_ha.py
# needs them, and that module skips itself when Home Assistant is absent, so
# this option is harmless in a checkout without the test harness installed.
asyncio_mode = auto
