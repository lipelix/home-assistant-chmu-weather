# Development dependencies
# ruff is pinned exactly: its default rule set changes between minor releases
# (0.16.0 broke CI), so CI, pre-commit and local runs must use the same version.
ruff==0.16.3
pre-commit>=3.5.0
pytest>=8.3.0
# Real Home Assistant plus its test fixtures, for tests/test_weather_ha.py.
# Pinned exactly: the harness tracks Home Assistant releases closely, so an
# unpinned version changes what CI runs against without a code change.
pytest-homeassistant-custom-component==0.13.316
requests>=2.31.0
